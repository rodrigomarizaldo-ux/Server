[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install pnpm && pnpm install"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm --filter @workspace/api-server run start"