{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "pnpm -C artifacts/api-server run build",
    "start": "node artifacts/api-server/dist/src/index.js"
  },
  "dependencies": {
    "cors": "^2",
    "drizzle-orm": "catalog:",
    "express": "^5",
    "jsonwebtoken": "^9.0.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "typescript": "~5.9.2"
  },
  "pnpm": { 
    "neverBuiltDependencies": ["esbuild"] 
  }
}